/*
$scope, customers, GlobalsService,
        orderTemplateFromOrderCreateModel,
        orderTemplates,
        volumeRoundingRuleLocator,
        $state,
        $http, $stateParams) {
        */

// // init error message & set it to null.

// $scope: populate lists: jobTypes, languages, currencies 

// instantiate model based on template and officeId provided in stateParams

// ... 